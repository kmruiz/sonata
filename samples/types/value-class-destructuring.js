"use strict";function price(amount,currency){let body={};body.class='price';body.amount=amount;body.currency=currency;return body;};console.log(print(price(42,'EUR')));console.log(print(price(42,'$')));function print(price){return (function () {let currency=price.currency;let amount=price.amount;if(currency==='$'){return currency+' '+amount}return amount+' '+currency})();};