"use strict";function price(amount,currency){let self={};self.class='price';self.amount=amount;self.currency=currency;return self;};console.log(print(price(42,'EUR')));console.log(print(price(42,'$')));function print(price){const r$ =(function () {let currency=price.currency;let amount=price.amount;if(currency==='$'){return currency+' '+amount}return amount+' '+currency})();;return r$;};