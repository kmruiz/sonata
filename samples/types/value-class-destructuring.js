"use strict";function price(amount,currency){var body={};body.class='price';body.amount=amount;body.currency=currency;return body;};console.log(print(price(42,'EUR')));console.log(print(price(42,'$')));function print(price){return (function () {var amount=price.amount;var currency=price.currency;var amount=price.amount;var currency=price.currency;if(currency==='$'){return currency+' '+amount}return amount+' '+currency})();};