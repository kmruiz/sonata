requires std.io
requires std.stream

entity class App() {
    let do() = {
        StreamFromFunction(Math.random).take(10).array().forEach(println)
        self.stop()
    }
}

let a = App()
a.do()
a.stop()