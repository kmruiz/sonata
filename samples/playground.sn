requires std.testing
requires std.io

let _ = Testing(TestRunner([], println))

_.describe('Test', let (it) = {
    it('should do', let (assert) = {
        assert('true is true', false)
    })
})

_.run()